name: Deploy Frontend (SWA - Static export)

on:
  push:
    branches: [main]
    paths: ["app/**", ".github/workflows/*.yml"]
  pull_request:
    branches: [main]
    paths: ["app/**"]

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Build locally
        working-directory: app
        run: |
          npm ci
          npm run build
          npm run export   # 生成 app/out

      - name: Deploy to SWA (upload prebuilt files)
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_RED_SAND_0D1794703 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          # 关键：app_location 指向构建产物目录，而不是源码
          app_location: app/out
          # 关键：不再提供 output_location，避免 Oryx 介入
          skip_app_build: true
          production_branch: main
